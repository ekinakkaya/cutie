#ifndef cutie_common_h
#define cutie_common_h

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

/* when this flag is defined, the vm asembles and prints each
   instruction right before executing it. */
#define DEBUG_TRACE_EXECUTION

#endif